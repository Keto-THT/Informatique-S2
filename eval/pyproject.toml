[project]
name = "eval"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "keanutoofa", email = "keanu.toofa@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",           
    "uvicorn[standard]",
    "sqlmodel",          
    "jinja2",              
    "websockets",        
]

[project.scripts]
eval = "eval:main"

